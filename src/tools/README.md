# tools 模块说明

该模块定义并实现 MCP Tools，是对外能力的核心接口层。

**主要用途**：向 MCP 客户端暴露学术文献与科研论文检索分析能力，包括多源搜索、全文获取、引用关系分析和期刊质量评估。

- `definitions.ts`：工具清单与元数据定义。
- `schemas.ts`：工具参数校验模式定义。
- `handlers.ts`：工具执行逻辑与结果组织。
- `index.ts`：工具注册与模块导出入口。
