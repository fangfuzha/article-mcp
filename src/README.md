# src 模块说明

该目录是 Node 版本 MCP 服务的核心源码入口。

**主要用途**：支撑学术文献与科研论文检索分析型 MCP 服务，重点覆盖医学、生命科学和跨学科科研场景，并兼容 arXiv 等通用预印本来源。

- `index.ts`：服务启动与整体装配入口，负责注册工具和中间件。
- `middleware/`：通用中间件能力（如缓存、执行管线）。
- `services/`：面向外部数据源的服务层封装。
- `tools/`：MCP Tool 的定义、参数模式与处理器实现。
- `utils/`：跨模块复用的基础工具函数。
